# Refactor Workflow

baseline tests
-> architecture
-> incremental implementation
-> tests
-> review
-> QA

The observable behavior should remain unchanged unless the specification
explicitly requires a behavior change.
